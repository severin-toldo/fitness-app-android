package com.stoldo.fitness_app_android.model.enums;

/**
 * Used for the itent params instead of strings. Easier to rename and update since it's everywhere the same.
 * */
public enum IntentParams {
    EXERCISE_ID,
    WORKOUT_ID;
}
